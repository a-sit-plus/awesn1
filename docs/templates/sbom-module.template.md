---
hide:
  - toc
---

# {{ module_title }} SBOM

The table below lists all published Maven publications for this module.

- `metadata` refers to the root `kotlinMultiplatform` publication
- `target` refers to a concrete published target artifact such as `jvm`, `android`, `iosArm64`, or `wasmJs`

Use the JSON/XML links for the exact CycloneDX SBOM attached to that publication.

| Component | Type | JSON SBOM | XML SBOM | Version |
| --- | --- | --- | --- | --- |
{{ table_rows }}
